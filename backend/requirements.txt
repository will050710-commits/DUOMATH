# ── Flask (server.py — stable) ──
flask>=3.0.0
flask-cors>=4.0.0
flask-jwt-extended>=4.6.0
flask-compress>=1.14
werkzeug>=3.0.0
requests>=2.31.0
gunicorn>=21.2.0

# ── FastAPI (main.py — async upgrade) ──
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
httpx>=0.27.0
PyJWT>=2.8.0

# ── Shared ──
orjson>=3.9.0
easyocr>=1.7.0
Pillow>=10.0.0
