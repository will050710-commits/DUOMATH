# ── Flask (server.py) ──
flask>=3.0.0
flask-cors>=4.0.0
flask-jwt-extended>=4.6.0
flask-compress>=1.14
werkzeug>=3.0.0
requests>=2.31.0
gunicorn>=21.2.0

# ── FastAPI (main.py — production) ──
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
httpx>=0.27.0
PyJWT[crypto]>=2.8.0
cryptography>=42.0.0


# ── Shared ──
orjson>=3.9.0
sympy>=1.12
numpy>=1.26
Pillow>=10.0.0
json-repair>=0.30.0

# ── Unlimited-OCR Spaces API & Multi-page PDF (lightweight client) ──
gradio_client>=2.0.0
pymupdf>=1.27.0

