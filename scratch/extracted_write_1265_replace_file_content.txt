{
  "AllowMultiple": "false",
  "Description": "\"Update game handlers and ELO logging hooks in PremiumLessonEngine.js\"",
  "EndLine": "431",
  "Instruction": "\"Update MC, TF, and Fill game handlers with useGamification triggers and logQuizAttempt ELO calls.\"",
  "ReplacementContent": "\"  // MC handlers\\n  const handleMcSelect = (i) => {\\n    if (mcSelected !== null) return;\\n    setMcSelected(i);\\n    const correct = i === activeMcQuestions[mcIndex].answer;\\n    if (correct) setMcScore((s) => s + 1);\\n    setMcHistory((h) => [...h, { q: mcIndex, selected: i, correct }]);\\n    \\n    // Log individual ELO attempt immediately\\n    const elapsedSeconds = Math.round((Date.now() - questionStartTimeRef.current) / 1000);\\n    logQuizAttempt({\\n      questionId: `${lessonSlug}_mc_${difficulty}_${mcIndex}`,\\n      topic: lessonSlug,\\n      difficulty,\\n      isCorrect: correct,\\n      timeTakenSec: elapsedSeconds\\n    }).then(res => {\\n      if (res.ok && res.data?.next_difficulty && difficultyMode === \\\"auto\\\") {\\n        setDifficulty(res.data.next_difficulty);\\n      }\\n    }).catch(console.error);\\n  };\\n\\n  const handleMcNext = () => {\\n    if (mcIndex + 1 >= activeMcQuestions.length) {\\n      setMcDone(true);\\n      const sessionAcc = Math.round((mcScore / activeMcQuestions.length) * 100);\\n      triggerGamification({\\n        questions: activeMcQuestions.map((q, idx) => ({\\n          id: `${lessonSlug}_mc_${difficulty}_${idx}`,\\n          isCorrect: idx === mcIndex ? (mcSelected === q.answer) : (mcHistory.find(h => h.q === idx)?.correct || false),\\n          difficulty\\n        })),\\n        timeTakenSec: Math.round((Date.now() - sessionStartTime.current) / 1000),\\n        topic: lessonSlug,\\n        sessionAccuracy: sessionAcc\\n      });\\n    } else {\\n      setMcIndex((i) => i + 1);\\n      setMcSelected(null);\\n    }\\n  };\\n\\n  // TF handlers\\n  const handleTfAnswer = (ans) => {\\n    if (tfFlipped) return;\\n    setTfFlipped(true);\\n    const correct = ans === activeTfCards[tfIndex].answer;\\n    if (correct) setTfScore((s) => s + 1);\\n    setTfHistory((h) => [...h, { q: tfIndex, given: ans, correct }]);\\n\\n    // Log individual ELO attempt immediately\\n    const elapsedSeconds = Math.round((Date.now() - questionStartTimeRef.current) / 1000);\\n    logQuizAttempt({\\n      questionId: `${lessonSlug\n<truncated 4472 bytes>",
  "StartLine": "333",
  "TargetContent": "\"  // MC handlers\\n  const handleMcSelect = (i) => {\\n    if (mcSelected !== null) return;\\n    setMcSelected(i);\\n    const correct = i === mcQuestions[mcIndex].answer;\\n    if (correct) setMcScore((s) => s + 1);\\n    setMcHistory((h) => [...h, { q: mcIndex, selected: i, correct }]);\\n  };\\n  const handleMcNext = () => {\\n    if (mcIndex + 1 >= mcQuestions.length) {\\n      setMcDone(true);\\n    } else {\\n      setMcIndex((i) => i + 1);\\n      setMcSelected(null);\\n    }\\n  };\\n  const resetMc = () => {\\n    setMcIndex(0);\\n    setMcSelected(null);\\n    setMcScore(0);\\n    setMcDone(false);\\n    setMcHistory([]);\\n  };\\n\\n  // TF handlers\\n  const handleTfAnswer = (ans) => {\\n    if (tfFlipped) return;\\n    setTfFlipped(true);\\n    const correct = ans === tfCards[tfIndex].answer;\\n    if (correct) setTfScore((s) => s + 1);\\n    setTfHistory((h) => [...h, { q: tfIndex, given: ans, correct }]);\\n  };\\n  const handleTfNext = () => {\\n    if (tfIndex + 1 >= tfCards.length) {\\n      setTfDone(true);\\n    } else {\\n      setTfIndex((i) => i + 1);\\n      setTfFlipped(false);\\n    }\\n  };\\n  const resetTf = () => {\\n    setTfIndex(0);\\n    setTfFlipped(false);\\n    setTfScore(0);\\n    setTfDone(false);\\n    setTfHistory([]);\\n  };\\n\\n  // Fill handlers\\n  const checkFill = (q) => {\\n    const raw = (fillAnswers[q.id] || \\\"\\\").toLowerCase().trim().replace(/\\\\s/g, \\\"\\\");\\n    return [q.answer, ...(q.altAnswers || [])].map((a) => a.toLowerCase().replace(/\\\\s/g, \\\"\\\")).includes(raw);\\n  };\\n  const fillScore = fillChecked ? fillQuestions.filter((q) => checkFill(q)).length : null;\\n\\n  // Formatting results\\n  const mcResultItems = mcHistory.map((h) => ({\\n    correct: h.correct,\\n    qText: mcQuestions[h.q].q,\\n    correctText: mcQuestions[h.q].options[mcQuestions[h.q].answer],\\n    yourText: mcQuestions[h.q].options[h.selected]\\n  }));\\n  const tfResultItems = tfHistory.map((h) => ({\\n    correct: h.correct,\\n    qText: tfCards[h.q].stmt,\\n    correctText: tfCards[h.q].answer ? t(\\\"ĐÚNG\\\", \\\"TRUE\\\") : t(\\\"SAI\\\", \\\"FALSE\\\"\n<truncated 1263 bytes>",
  "TargetFile": "\"c:\\\\Users\\\\Latitude 7300\\\\OneDrive\\\\Máy tính\\\\duosteam - Copy\\\\duosteam\\\\frontend\\\\src\\\\components\\\\Cacbaitoan10\\\\PremiumLessonEngine.js\"",
  "toolAction": "\"Refactoring game event handlers\"",
  "toolSummary": "\"Edit file content\""
}